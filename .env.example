# Copia este archivo a .env para desarrollo local, o usa .env.demo.example /
# .env.prod.example como base para esos ambientes. Ver README.md >
# "Variables de entorno" y "Ambientes (demo / producción)".

# --- Proveedor de LLM ---
LLM_PROVIDER=gemini
GEMINI_API_KEY=YOUR.KEY

# --- Base de datos ---
# DB_PROVIDER controla el 'provider' del datasource de Prisma (ver
# scripts/prepare_prisma_schema.py). Valores validos: "sqlite" o
# "postgresql". Debe ejecutarse `python scripts/prepare_prisma_schema.py`
# despues de cambiar este valor y ANTES de `prisma generate` / `prisma db push`.
DB_PROVIDER=sqlite
DATABASE_URL=file:./app.db

# --- Proveedores adicionales (opcional) ---
# ANTHROPIC_API_KEY=YOUR.KEY
# OPENAI_API_KEY=YOUR.KEY
# DEEPSEEK_API_KEY=YOUR.KEY
# GITHUB_API_KEY=YOUR.GITHUB.TOKEN
# GROQ_API_KEY=YOUR.KEY

# --- Fallback automatico (opcional) ---
# Si el proveedor activo (LLM_PROVIDER, siempre gemini) falla, ask_llm()
# reintenta automaticamente con este proveedor (debe tener su propia clave
# configurada arriba). Util para no interrumpir una demo si un proveedor se
# queda sin cuota. Valores validos: claude, openai, gemini, deepseek, github, groq.
# LLM_FALLBACK_PROVIDER=groq
